import React from "react";
import Section from "../Layout/Section";
import { SectionHeader } from "../Layout/styles";

const Activities = () => {
  return (
    <Section id="activities" className="bg-map-beige min-h-screen">
      <SectionHeader className="text-map-red">Activities</SectionHeader>
    </Section>
  );
};

export default Activities;
