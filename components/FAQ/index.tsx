import React from "react";
import Section from "../Layout/Section";
import { SectionHeader } from "../Layout/styles";

const FAQ = () => {
  return (
    <Section id="faq" className="bg-map-green min-h-screen">
      <SectionHeader className="text-map-red">FAQ</SectionHeader>
    </Section>
  );
};

export default FAQ;
