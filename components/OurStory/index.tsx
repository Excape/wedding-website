import React from "react";
import Section from "../Layout/Section";
import { SectionHeader } from "../Layout/styles";

const OurStory = () => {
  return (
    <Section id="ourstory" className="bg-map-beige-light min-h-screen">
      <SectionHeader className="text-map-blue">Our Story</SectionHeader>
    </Section>
  );
};

export default OurStory;
