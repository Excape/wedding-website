import React from "react";
import Section from "../Layout/Section";
import { SectionHeader } from "../Layout/styles";

const Registry = () => {
  return (
    <Section id="registry" className="bg-map-beige-light min-h-screen">
      <SectionHeader className="text-map-blue">Registry</SectionHeader>
    </Section>
  );
};

export default Registry;
